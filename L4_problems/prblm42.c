/** Get the string and find the length of the string 
 * */
#include<stdio.h>
#include<string.h>
void main(){
char arr[20],len=0;
printf("Enter the string :");
scanf("%s",arr);
printf("Teh string len is %d ",strlen(arr));
while(arr[len++]){
}
printf("Length of the entered string is %d:",len-1);
}
